% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/l_testing.R
\name{l.cdf_glmnet}
\alias{l.cdf_glmnet}
\title{Evaluate the ell-CDF at a point}
\usage{
l.cdf_glmnet(
  x,
  y,
  X,
  ind,
  lambda,
  lambda_cv,
  glmnet_object = NULL,
  glmnet_object_type = 1,
  adjusted = FALSE,
  return_both = FALSE,
  return_dropprob = FALSE
)
}
\arguments{
\item{x}{numeric. Point at which the ell-CDF is to be evaluated.}

\item{y}{numeric vector. Response variable.}

\item{X}{matrix or data.frame. Full column-rank, \verb{n x p} design matrix; \code{nrow(X)} must equal \code{length(y)}.}

\item{ind}{integer. Index of the coefficient to test; by default an intercept is \strong{not} added to \code{X}.}

\item{lambda}{numeric. Lambda for the selection-LASSO: a positive value, or one of
\itemize{
\item \code{-1} to choose \code{lambda.min} with \code{cv.glmnet()} called on \verb{(y, X[,-ind])},
\item \code{-2} to choose \code{lambda.1se} with \code{cv.glmnet()} called on \verb{(y, X[,-ind])},
\item \code{-3} to choose \code{lambda.min} with \code{cv.glmnet()} called on \verb{(y, X)},
\item \code{-4} to choose \code{lambda.1se} with \code{cv.glmnet()} called on \verb{(y, X)}. Default \code{-1}.
}}

\item{lambda_cv}{numeric. Lambda used for evaluating the ell-distribution, same coding as \code{lambda}. Default \code{-1}.}

\item{glmnet_object}{list or \code{NULL}. If a list, then either of length-2 with object returned by \code{cv.glmnet()} on \verb{(y, X)} and \verb{(y, X[,-ind])} as first and second components,
or length \code{d+1} list where the first component is the object returned by \code{cv.glmnet()} called on \verb{(y, X)} and the \code{i}th is when called on \verb{(y, X[,-i])}. Default \code{NULL}.}

\item{glmnet_object_type}{integer. Set this to \code{1} if \code{glmnet_object} is in the 2-element list form, \code{1} for the \code{d+1}-element form. If \code{glmnet_object} is \code{NULL}, this is forced to \code{1}. Default \code{1}.}

\item{adjusted}{logical. If \code{TRUE}, return the CDF conditional on LASSO selecting variable \code{ind}. Default \code{FALSE}.}

\item{return_both}{logical. If \code{TRUE}, return both unconditional and conditional CDFs. Default \code{FALSE}.}

\item{return_dropprob}{logical. If \code{TRUE}, also return the conditional drop probability (LASSO estimate at \code{ind} equals 0). Default \code{FALSE}.}
}
\value{
If \code{return_dropprob = FALSE}, returns a numeric vector \code{v}. If \code{TRUE}, returns a list with \code{v} as the first and the conditional drop probability as the second component.
When \code{return_both = FALSE}, \code{v} is a single value (unconditional value if \code{adjusted = FALSE}, conditional value if \code{adjusted = TRUE}).
When \code{return_both = TRUE}, \code{v} has two entries: \verb{(unconditional value of CDF, conditional value of CDF)}. If LASSO does not select variable \code{ind}, the conditional entry is \code{-1}.
}
\description{
Evaluate the ell-CDF at a point
}
